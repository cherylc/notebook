<li id="note_<%= note.id %>" class="note" data-nid="<%= note.id %>">
  <%= note.content %>

  <%= link_to image_tag('trash-can-icon.jpg', height: 20),
              note_url(note), method: 'delete', remote: true %>
</li>
