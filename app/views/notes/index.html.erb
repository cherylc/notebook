<ul>

<% @notes.each do |n| %>
  <%= render 'note_row', :note => n %>
<% end %>

</ul>

<%= form_for(Note.new, remote: true) do |f| %>
    <%= f.text_field :content, placeholder: 'Add a new aha! here' %>
<% end %>
